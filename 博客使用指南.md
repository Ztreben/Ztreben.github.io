# 🎉 Butterfly 博客完全使用指南

> 写给零基础小白的保姆级教程 - 让你轻松玩转 Hexo 博客！

---

## 📖 目录

1. [日常写作流程](#日常写作流程)
2. [常用命令速查](#常用命令速查)
3. [主题美化优化](#主题美化优化)
4. [新功能扩展](#新功能扩展)
5. [常见问题解决](#常见问题解决)

---

## ✍️ 日常写作流程

### 第一步：创建新文章

在终端（VS Code 里按 `` Ctrl+` `` 打开）输入：

```bash
cd /Users/zhangzezhen/MyBlog/hexo-site
npx hexo new "我的文章标题"
```

**会发生什么？**
- Hexo 会在 `hexo-site/source/_posts/` 文件夹创建一个新的 Markdown 文件
- 文件名类似 `我的文章标题.md`

### 第二步：编辑文章内容

1. 在 VS Code 左侧找到 `hexo-site/source/_posts/我的文章标题.md`
2. 双击打开，看到类似这样的内容：

```markdown
---
title: 我的文章标题
date: 2025-11-14 15:30:00
tags:
categories:
---

在这里开始写正文...
```

**重要提示：**
- `---` 之间的内容叫 **Front-Matter**（文章元信息）
- `tags:` 后面可以写标签，例如 `tags: [Python, 学习笔记]`
- `categories:` 写分类，例如 `categories: 编程`
- 正文用 Markdown 语法书写（标题用 `#`，代码块用 ` ``` `）

### 第三步：本地预览

```bash
cd /Users/zhangzezhen/MyBlog/hexo-site
npx hexo server
```

然后在浏览器打开 **http://localhost:4000** 查看效果

**小技巧：**
- 修改文章后保存，浏览器会自动刷新（热更新）
- 按 `Ctrl+C` 停止预览服务器

### 第四步：发布到网站

确认文章没问题后，运行：

```bash
cd /Users/zhangzezhen/MyBlog
bash publish.sh
```

**会自动完成：**
1. ✅ 生成静态网页
2. ✅ 提交到 GitHub
3. ✅ 部署到 https://ztreben.github.io

**等待 1-2 分钟**，刷新你的网站就能看到新文章了！

---

## 🚀 常用命令速查

| 操作 | 命令 | 说明 |
|------|------|------|
| 创建文章 | `npx hexo new "标题"` | 在 `_posts/` 生成新文件 |
| 本地预览 | `npx hexo server` | 打开 http://localhost:4000 |
| 生成静态文件 | `npx hexo generate` | 生成 `public/` 文件夹 |
| 清理缓存 | `npx hexo clean` | 删除旧的生成文件 |
| 发布网站 | `bash publish.sh` | 一键部署到 GitHub Pages |
| 快速发布（跳过清理） | `bash publish.sh --no-clean` | 加速发布 |

**记住这 3 个核心步骤：**
```bash
# 1. 进入 hexo-site 目录
cd /Users/zhangzezhen/MyBlog/hexo-site

# 2. 写文章 + 预览
npx hexo new "文章标题"
npx hexo server

# 3. 发布上线
cd ..
bash publish.sh
```

---

## 🎨 主题美化优化

### 优先级 ⭐⭐⭐（强烈推荐）

#### 1. 添加头像和网站图标

**操作步骤：**

```bash
# 1. 创建图片文件夹
mkdir -p /Users/zhangzezhen/MyBlog/hexo-site/source/images

# 2. 把你的头像图片放进去，命名为 avatar.png
# 可以拖拽图片到这个文件夹，或用 Finder 打开
```

**然后编辑配置：**

打开 `hexo-site/_config.butterfly.yml`，找到 `avatar:` 部分，修改为：

```yaml
avatar:
  img: /images/avatar.png
  rounded: true  # 圆形头像
  effect: true   # 鼠标悬停旋转效果
```

#### 2. 自定义网站信息

编辑 `hexo-site/_config.yml`（主配置文件），修改：

```yaml
# 网站基本信息
title: 你的博客名称（例如：张三的技术笔记）
subtitle: 一句话描述（例如：记录学习，分享成长）
description: 更详细的描述
author: 你的名字
language: zh-CN

# 网站链接（改成你的实际地址）
url: https://ztreben.github.io
```

#### 3. 开启深色模式

编辑 `_config.butterfly.yml`，找到或添加：

```yaml
# 深色模式（跟随系统自动切换）
darkmode:
  enable: true
  button: true  # 显示切换按钮
  autoChangeMode: 1  # 1=跟随系统，2=固定深色，3=固定浅色
```

#### 4. 自定义菜单

编辑 `_config.butterfly.yml`：

```yaml
menu:
  首页: / || fas fa-home
  归档: /archives/ || fas fa-archive
  标签: /tags/ || fas fa-tags
  分类: /categories/ || fas fa-folder-open
  关于: /about/ || fas fa-user
```

**创建对应页面：**

```bash
cd /Users/zhangzezhen/MyBlog/hexo-site

# 创建标签页
npx hexo new page tags
# 编辑 source/tags/index.md，添加：
# ---
# title: 标签
# type: tags
# ---

# 创建分类页
npx hexo new page categories
# 编辑 source/categories/index.md，添加：
# ---
# title: 分类
# type: categories
# ---

# 创建关于页
npx hexo new page about
# 在 source/about/index.md 写你的自我介绍
```

---

### 优先级 ⭐⭐（推荐添加）

#### 5. 添加社交链接

编辑 `_config.butterfly.yml`：

```yaml
social:
  fab fa-github: https://github.com/Ztreben || GitHub
  fas fa-envelope: mailto:你的邮箱@example.com || 邮箱
  fab fa-weixin: javascript:; || 微信（点击可弹出二维码）
  fab fa-qq: tencent://message/?uin=你的QQ号 || QQ
```

#### 6. 开启本地搜索

已经配置好了！在网站右上角会有搜索按钮 🔍

#### 7. 启用代码高亮美化

编辑 `_config.butterfly.yml`：

```yaml
highlight_theme: mac  # 可选：default / mac / mac light / github
highlight_copy: true  # 代码复制按钮
highlight_lang: true  # 显示语言标签
highlight_shrink: false  # 代码块默认展开
```

#### 8. 开启图片灯箱（点击放大）

编辑 `_config.butterfly.yml`：

```yaml
fancybox: true
```

现在文章里的图片点击后会放大显示！

#### 9. 文章封面图

在每篇文章的 Front-Matter 添加：

```markdown
---
title: 我的文章
date: 2025-11-14
cover: https://source.unsplash.com/random/800x400  # 随机图片
# 或使用本地图片：cover: /images/封面.jpg
---
```

#### 10. 美化侧边栏

编辑 `_config.butterfly.yml`：

```yaml
aside:
  enable: true
  position: right  # left 或 right
  card_author:
    enable: true
    description: 一句话介绍自己
    button:
      enable: true
      text: 关于我
      link: /about/
  card_recent_post:
    enable: true
    limit: 5  # 显示最近 5 篇文章
  card_categories:
    enable: true
    limit: 8
  card_tags:
    enable: true
    limit: 40
    color: true  # 彩色标签
```

---

### 优先级 ⭐（进阶功能）

#### 11. 添加评论系统（推荐 Waline）

**为什么用 Waline？**
- ✅ 免费
- ✅ 支持 Markdown
- ✅ 有表情包
- ✅ 邮件通知

**快速部署：**

1. 访问 https://waline.js.org/guide/get-started/
2. 点击 "Vercel 部署" 按钮，用 GitHub 登录
3. 获得评论服务器地址（类似 `https://你的项目.vercel.app`）

**配置博客：**

编辑 `_config.butterfly.yml`：

```yaml
comments:
  use: Waline
  
waline:
  serverURL: https://你的项目.vercel.app
  avatar: monsterid
  emoji: true
  pageSize: 10
```

#### 12. 网站访问统计（不蒜子）

编辑 `_config.butterfly.yml`：

```yaml
busuanzi:
  site_uv: true  # 总访客数
  site_pv: true  # 总访问量
  page_pv: true  # 文章阅读数
```

#### 13. 添加音乐播放器

编辑 `_config.butterfly.yml`：

```yaml
aplayer:
  enable: true
  audio:
    - name: 歌曲名称
      artist: 歌手
      url: 音乐链接
      cover: 封面图片链接
```

#### 14. 打字机效果（首页副标题）

编辑 `_config.butterfly.yml`：

```yaml
subtitle:
  enable: true
  sub:
    - 欢迎来到我的博客！
    - 记录学习，分享生活
    - Stay hungry, stay foolish.
```

#### 15. 文章置顶

在文章的 Front-Matter 添加：

```markdown
---
title: 重要文章
sticky: 100  # 数字越大越靠前
---
```

---

## 🔧 新功能扩展

### 推荐安装的插件

#### 1. 文章字数统计

```bash
cd /Users/zhangzezhen/MyBlog/hexo-site
npm install hexo-wordcount --save
```

编辑 `_config.butterfly.yml`：

```yaml
wordcount:
  enable: true
  post_wordcount: true  # 文章字数
  min2read: true  # 阅读时长
```

#### 2. 站点地图（利于 SEO）

```bash
npm install hexo-generator-sitemap --save
```

编辑主配置 `_config.yml`：

```yaml
sitemap:
  path: sitemap.xml
```

#### 3. RSS 订阅（已安装）

已经配置好了！订阅地址：`https://ztreben.github.io/atom.xml`

#### 4. 图片懒加载

编辑 `_config.butterfly.yml`：

```yaml
lazyload:
  enable: true
  onlypost: false
```

#### 5. Pjax 无刷新加载

编辑 `_config.butterfly.yml`：

```yaml
pjax:
  enable: true
```

**注意：** 开启后页面切换更流畅，但某些 JS 功能可能需要调整。

---

## 💡 常见问题解决

### Q1: 本地预览正常，但发布后样式乱了？

**解决办法：**

```bash
cd /Users/zhangzezhen/MyBlog/hexo-site
npx hexo clean
npx hexo generate
cd ..
bash publish.sh
```

### Q2: 图片显示不出来？

**检查清单：**
1. 图片路径是否正确？（本地图片放在 `source/images/`）
2. 使用 `/images/图片名.jpg`（注意开头有 `/`）
3. 外链图片是否被墙？建议用国内图床（如七牛云、阿里云 OSS）

### Q3: 发布后要等多久才能看到更新？

通常 **1-3 分钟**。如果超过 5 分钟：
1. 清除浏览器缓存（`Ctrl+Shift+R` 强制刷新）
2. 检查 GitHub Actions 是否有错误（访问仓库的 Actions 标签）

### Q4: 不小心删除了重要文件怎么办？

**别慌！Git 能救你：**

```bash
cd /Users/zhangzezhen/MyBlog
git status  # 查看被删除的文件
git checkout -- 文件路径  # 恢复单个文件
git reset --hard HEAD  # 恢复所有文件到上次提交（慎用！）
```

### Q5: 想换回旧主题？

编辑 `hexo-site/_config.yml`，把 `theme: butterfly` 改回 `theme: landscape`，然后：

```bash
cd /Users/zhangzezhen/MyBlog
bash publish.sh
```

### Q6: 修改配置后不生效？

**必须清理缓存：**

```bash
cd /Users/zhangzezhen/MyBlog/hexo-site
npx hexo clean
npx hexo generate
```

---

## 📝 Markdown 写作速查

### 标题

```markdown
# 一级标题
## 二级标题
### 三级标题
```

### 强调

```markdown
**粗体**
*斜体*
~~删除线~~
`代码`
```

### 列表

```markdown
- 无序列表项 1
- 无序列表项 2

1. 有序列表项 1
2. 有序列表项 2
```

### 链接和图片

```markdown
[链接文字](https://example.com)
![图片描述](/images/图片.jpg)
```

### 代码块

```markdown
​```python
def hello():
    print("Hello World!")
​```
```

### 引用

```markdown
> 这是一段引用文字
```

### 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 内容1 | 内容2 | 内容3 |
```

---

## 🎯 推荐优化顺序

### 第一天（基础美化）
1. ✅ 添加头像和网站图标
2. ✅ 修改网站标题和描述
3. ✅ 自定义菜单（首页、归档、标签、分类）
4. ✅ 开启深色模式

### 第二天（内容完善）
1. ✅ 创建"关于"页面，介绍自己
2. ✅ 写 2-3 篇示例文章
3. ✅ 给文章添加标签和分类
4. ✅ 测试本地搜索功能

### 第三天（功能增强）
1. ✅ 添加社交链接
2. ✅ 开启评论系统（Waline）
3. ✅ 配置代码高亮主题
4. ✅ 启用图片灯箱

### 第四天（高级优化）
1. ✅ 安装字数统计插件
2. ✅ 开启访问统计（不蒜子）
3. ✅ 优化侧边栏显示
4. ✅ 自定义 CSS 微调样式

---

## 📚 学习资源

- **Hexo 官方文档**：https://hexo.io/zh-cn/docs/
- **Butterfly 主题文档**：https://butterfly.js.org/
- **Markdown 语法教程**：https://www.markdown.xyz/basic-syntax/
- **GitHub Pages 指南**：https://docs.github.com/cn/pages

---

## 🆘 需要帮助？

1. **主题问题**：访问 Butterfly 官方文档或 GitHub Issues
2. **Hexo 问题**：搜索 Hexo 官方文档
3. **Git 问题**：Google 搜索 "Git [你的问题]"
4. **急需支持**：在 VS Code 里问 GitHub Copilot！

---

## 🎉 最后的话

**记住这句话：**
> 博客是记录成长的工具，不是负担。想写就写，不要有压力！

**保持简单的习惯：**
1. 有想法 → 写文章
2. 本地预览 → 确认效果
3. 一键发布 → 等待上线

**祝你写博客愉快！🚀**

---

**文档版本：** v1.0  
**最后更新：** 2025-11-14  
**如有疑问，随时问 GitHub Copilot！** 😊
